name: Automated CI/CD Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18.x"

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: echo "Skipping tests for now"

      - name: Debug Secrets
        run: |
          echo "Checking if secrets are available..."
          if [[ -z "${{ secrets.RENDER_API_KEY }}" ]]; then
            echo "❌ RENDER_API_KEY is missing!"
            exit 1
          else
            echo "✅ RENDER_API_KEY is set!"
          fi

          if [[ -z "${{ secrets.RENDER_SERVICE_ID }}" ]]; then
            echo "❌ RENDER_SERVICE_ID is missing!"
            exit 1
          else
            echo "✅ RENDER_SERVICE_ID is set!"
          fi

      - name: Validate all required secrets
        run: |
          missing=""

           if [ -z "${{ secrets.CLOUD_NAME }}" ]; then echo "❌ CLOUD_NAME is missing"; missing="yes"; fi
           if [ -z "${{ secrets.CLOUD_KEY }}" ]; then echo "❌ CLOUD_KEY is missing"; missing="yes"; fi
           if [ -z "${{ secrets.CLOUD_SECRET }}" ]; then echo "❌ CLOUD_SECRET is missing"; missing="yes"; fi
           if [ -z "${{ secrets.VDOCIPHER_API_SECRET }}" ]; then echo "❌ VDOCIPHER_API_SECRET is missing"; missing="yes"; fi
           if [ -z "${{ secrets.ACCESS_TOKEN_SIGN_IN }}" ]; then echo "❌ ACCESS_TOKEN_SIGN_IN is missing"; missing="yes"; fi

           if [ "$missing" = "yes" ]; then
             echo "🚨 Exiting due to missing secrets."
             exit 1
           else
             echo "✅ All required secrets are set."
           fi

      - name: Build Docker image
        run: docker build -t e-learning-server .

      - name: Run Docker container with GitHub Secrets
        run: |
          docker run --rm \
            -e PORT=${{ secrets.PORT }} \
            -e ORIGIN=${{ secrets.ORIGIN }} \
            -e NODE_ENV=${{ secrets.NODE_ENV }} \
            -e DB_URL=${{ secrets.DB_URL }} \
            -e CLOUD_NAME=${{ secrets.CLOUD_NAME }} \
            -e CLOUD_KEY=${{ secrets.CLOUD_KEY }} \
            -e CLOUD_SECRET=${{ secrets.CLOUD_SECRET }} \
            -e REDIS_URL=${{ secrets.REDIS_URL }} \
            -e ACTIVATION_SECRET=${{ secrets.ACTIVATION_SECRET }} \
            -e ACCESS_TOKEN_SIGN_IN=${{ secrets.ACCESS_TOKEN_SIGN_IN }} \
            -e REFRESH_TOKEN_SIGN_IN=${{ secrets.REFRESH_TOKEN_SIGN_IN }} \
            -e ACCESS_TOKEN_EXPIRE=${{ secrets.ACCESS_TOKEN_EXPIRE }} \
            -e REFRESH_TOKEN_EXPIRE=${{ secrets.REFRESH_TOKEN_EXPIRE }} \
            -e SMTP_HOST=${{ secrets.SMTP_HOST }} \
            -e SMTP_PORT=${{ secrets.SMTP_PORT }} \
            -e SMTP_SERVICE=${{ secrets.SMTP_SERVICE }} \
            -e SMTP_MAIL=${{ secrets.SMTP_MAIL }} \
            -e SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }} \
            -e VDOCIPHER_API_SECRET=${{ secrets.VDOCIPHER_API_SECRET }} \
            -e STRIPE_PUBLISHABLE_KEY=${{ secrets.STRIPE_PUBLISHABLE_KEY }} \
            -e STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }} \
            "e-learning-server"

      - name: Confirm Successful build
        if: success()
        run: echo "Build job success"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      deployments: write
    steps:
      - name: Deploy to Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{secrets.RENDER_SERVICE_ID}}
          api-key: ${{secrets.RENDER_API_KEY}}

      - name: Check Deployment Response
        if: failure() # This will only run if the previous step fails
        run: |
          echo "❌ Deployment failed! Exiting..."
          exit 1

      - name: Confirm Deployment Success
        if: success() # This ensures it only runs on success
        run: echo "✅ Deployment completed successfully!"
#       name: Automated CI/CD Pipeline

# on:
#   push:
#     branches:
#       - master
#   pull_request:
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     services:
#       redis:
#         image: redis:6
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up Node.js
#         uses: actions/setup-node@v2
#         with:
#           node-version: "18.x"

#       - name: Create .env file
#         run: |
#           echo "PORT=3000" >> .env
#           echo "REDIS_URL=redis://localhost:6379" >> .env
#           echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env

#       - name: Install dependencies
#         run: npm install

#       - name: Run build
#         run: npm run build

#       - name: Run Docker container
#         run: |
#           docker build -t my-app .
#           docker run --rm \
#             --env-file .env \
#             my-app

#       - name: Confirm Successful Build
#         if: success()
#         run: echo "✅ Build and container run completed"

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     permissions:
#       deployments: write

#     steps:
#       - name: Deploy to Render
#         uses: johnbeynon/render-deploy-action@v0.0.8
#         with:
#           service-id: ${{ secrets.RENDER_SERVICE_ID }}
#           api-key: ${{ secrets.RENDER_API_KEY }}

#       - name: Check Deployment Response
#         if: failure()
#         run: |
#           echo "❌ Deployment failed! Exiting..."
#           exit 1

#       - name: Confirm Deployment Success
#         if: success()
#         run: echo "✅ Deployment completed successfully!"
